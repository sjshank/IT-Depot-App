import { NextPageWithLayout } from "@/layout";
import WithPageLayout from "@/hoc/withPageLayout";

const TicketDetailsPage: NextPageWithLayout = () => {
  return <>TicketDetailsPage</>;
};

export default WithPageLayout(TicketDetailsPage);
