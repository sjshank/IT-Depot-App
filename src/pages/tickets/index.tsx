import { NextPageWithLayout } from "@/layout";
import WithPageLayout from "@/hoc/withPageLayout";

const TicketsPage: NextPageWithLayout = () => {
  return <>TicketsPage</>;
};

export default WithPageLayout(TicketsPage);
